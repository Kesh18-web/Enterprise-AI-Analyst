{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "HOSTNAME=0.0.0.0 PORT=$PORT node server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
